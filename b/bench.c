

void test_special()
{
    double inmat[SIZE][SIZE] = {{2, 6, 1}, {8, 7, 9}, {2, 0, 2}};
    double exmat[SIZE][2 * SIZE];
    double determinant;
    int rank;
    // gauss(inmat, exmat, &rank, &determinant);
}

